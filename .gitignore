# .NET
bin/
obj/
*.user
*.suo

# Rider / VS
.vs/
.idea/
*.DotSettings.user

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml

# Node / Web (ForgeVault.Web, when it exists)
node_modules/
dist/
.vite/
*.tsbuildinfo
# `tsc -b` compiles vite.config.ts as a side effect of the composite tsconfig.node.json
# project reference — these are build output, not source.
/src/ForgeVault.Web/vite.config.js
/src/ForgeVault.Web/vite.config.d.ts

# Secrets — never committed
.env
.env.*
!.env.example
*.key
/root/.forgevault/

# OS
.DS_Store
Thumbs.db

# Claude Code local session artifacts
.claude/
